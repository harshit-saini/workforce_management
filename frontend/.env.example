# Absolute URL of the deployed backend API (include /api).
# Leave unset for local dev / same-origin deployments — it falls back to the relative "/api" path.
VITE_API_URL=https://your-backend.onrender.com/api
